/// <reference types="react" />
interface IProps {
    text: string;
    id: string;
    success: boolean;
    icon: string;
}
export declare const CustomModal: ({ text, id, success, icon }: IProps) => JSX.Element;
export {};
